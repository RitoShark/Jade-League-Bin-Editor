<Window x:Class="Jade.Windows.CompareDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="Compare Files" Height="700" Width="1200"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        ShowInTaskbar="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
                      ResizeBorderThickness="6"
                      GlassFrameThickness="0,0,0,1"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    
    
    <Window.Resources>
        <!-- Scrollbar Color Resources -->
        <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#3E3E42"/>
        <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#686868"/>
        <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#9E9E9E"/>
        
        <!-- ComboBox Color Resources -->
        <SolidColorBrush x:Key="ComboBoxBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="ComboBoxForeground" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="ComboBoxBorder" Color="#3E3E42"/>
        <SolidColorBrush x:Key="ComboBoxHoverBackground" Color="#3E3E42"/>
        
        <!-- Scrollbar Styles -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder" Background="{DynamicResource ScrollBarThumbBrush}" CornerRadius="4"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{DynamicResource ScrollBarTrackBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="12"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid>
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                                    <Track x:Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="12"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C97EA"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboBoxBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ComboBoxForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ComboBoxBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"
                                                    Fill="{DynamicResource ComboBoxForeground}"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ComboBoxHoverBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,5,28,5"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="200">
                                    <Border x:Name="DropDownBorder"
                                          Background="{DynamicResource ComboBoxBackground}"
                                          BorderBrush="{DynamicResource ComboBoxBorder}"
                                          BorderThickness="1">
                                        <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ComboBoxForeground}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                              Background="{TemplateBinding Background}"
                              Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ComboBoxHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ComboBoxHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid x:Name="RootGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border x:Name="TitleBar" Grid.Row="0" Background="#252526" Height="30" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Compare Files" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center" Margin="10,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="—" Width="46" Height="30" Click="MinimizeWindow" 
                                Background="Transparent" Foreground="#CCCCCC" BorderThickness="0"
                                FontSize="14" VerticalContentAlignment="Center"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                        <Button Content="□" Width="46" Height="30" Click="MaximizeWindow" 
                                Background="Transparent" Foreground="#CCCCCC" BorderThickness="0"
                                FontSize="14" VerticalContentAlignment="Center"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                        <Button Content="✕" Width="46" Height="30" Click="CloseWindow" 
                                Background="Transparent" Foreground="#CCCCCC" BorderThickness="0"
                                FontSize="14" VerticalContentAlignment="Center"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- File Selection and Navigation -->
            <Grid x:Name="FileSelectionGrid" Grid.Row="1" Margin="0,1,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Left:" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="10,0,8,0"/>
                <ComboBox Grid.Column="1" x:Name="LeftFileComboBox" MinWidth="200" Margin="0,8" SelectionChanged="OnFileSelectionChanged"/>
                
                <Border Grid.Column="2" Width="1" Background="#3E3E42" Margin="10,5"/>
                
                <Button Grid.Column="3" Content="◀ Previous" Click="OnPreviousDifference" Margin="10,8,5,8" Padding="10,5" FontSize="12"/>
                <Button Grid.Column="4" Content="Next ▶" Click="OnNextDifference" Margin="5,8,10,8" Padding="10,5" FontSize="12"/>
                
                <ComboBox Grid.Column="5" x:Name="RightFileComboBox" MinWidth="200" Margin="0,8" SelectionChanged="OnFileSelectionChanged"/>
                <TextBlock Grid.Column="6" Text="Right:" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="8,0,10,0"/>
            </Grid>
            
            <!-- Merge and Save Toolbar -->
            <Grid Grid.Row="2" Background="#2D2D30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left side buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4">
                    <Button x:Name="SaveLeftButton" Content="Save Left" Click="OnSaveLeft" Margin="5,0" Padding="10,4" FontSize="11" Background="#3E8E3E"/>
                    <Button Content="◀ Merge Selected" Click="OnMergeSelectedLeft" Margin="5,0" Padding="10,4" FontSize="11" ToolTip="Copy current diff from right to left"/>
                    <Button Content="◀◀ Merge All" Click="OnMergeAllLeft" Margin="5,0" Padding="10,4" FontSize="11" ToolTip="Copy all content from right to left"/>
                </StackPanel>
                
                <!-- Center label -->
                <TextBlock Grid.Column="1" Text="Merge" Foreground="#888888" VerticalAlignment="Center" Margin="15,0" FontSize="11"/>
                
                <!-- Right side buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4">
                    <Button Content="Merge All ▶▶" Click="OnMergeAllRight" Margin="5,0" Padding="10,4" FontSize="11" ToolTip="Copy all content from left to right"/>
                    <Button Content="Merge Selected ▶" Click="OnMergeSelectedRight" Margin="5,0" Padding="10,4" FontSize="11" ToolTip="Copy current diff from left to right"/>
                    <Button x:Name="SaveRightButton" Content="Save Right" Click="OnSaveRight" Margin="5,0" Padding="10,4" FontSize="11" Background="#3E8E3E"/>
                </StackPanel>
            </Grid>
            
            <!-- Comparison View -->
            <Grid Grid.Row="3" Margin="0,1,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left Editor -->
                <Border x:Name="LeftEditorBorder" Grid.Column="0" Background="#1E1E1E" BorderThickness="0">
                    <avalonedit:TextEditor x:Name="LeftEditor"
                                          FontFamily="Consolas"
                                          FontSize="14"
                                          ShowLineNumbers="True"
                                          IsReadOnly="False"
                                          Background="Transparent"
                                          Foreground="#CCCCCC"
                                          Padding="10"
                                          BorderThickness="0"/>
                </Border>
                
                <!-- Separator -->
                <Border Grid.Column="1" Width="2" Background="#3E3E42"/>
                
                <!-- Right Editor -->
                <Border x:Name="RightEditorBorder" Grid.Column="2" Background="#1E1E1E" BorderThickness="0">
                    <avalonedit:TextEditor x:Name="RightEditor"
                                          FontFamily="Consolas"
                                          FontSize="14"
                                          ShowLineNumbers="True"
                                          IsReadOnly="False"
                                          Background="Transparent"
                                          Foreground="#CCCCCC"
                                          Padding="10"
                                          BorderThickness="0"/>
                </Border>
            </Grid>
            
            <!-- Status Bar -->
            <Border x:Name="StatusBarBorder" Grid.Row="4" Height="24" Background="#007ACC">
                <TextBlock x:Name="StatusText" 
                           Text="Select files to compare" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="10,0"/>
            </Border>
    </Grid>
</Window>
